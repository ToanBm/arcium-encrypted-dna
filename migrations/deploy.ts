// Migrations are an early Anchor feature — left as placeholder.
// Deploy via: arcium deploy --cluster-offset 456 --keypair-path ~/.config/solana/id.json \
//   --recovery-set-size 5 --program-keypair target/deploy/encrypted_dna-keypair.json \
//   --program-name encrypted_dna --rpc-url devnet
module.exports = async function () {
  console.log("Use `arcium deploy` to deploy this program.");
};
